{
    "builds": [
        {
            "src": "api/app.py",
            "use": "@vercel/python",
            "config": {
                "includeFiles": "frontend/**"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/app.py"
        }
    ]
}
